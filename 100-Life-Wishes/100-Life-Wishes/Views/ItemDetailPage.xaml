<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="_100_Life_Wishes.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Text:" FontSize="Medium" />
        <Label Text="{Binding Text}" FontSize="Small"/>
        <Label Text="Description:" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>

        <StackLayout>
            <Entry Text="{Binding Text, Mode=TwoWay}" />
            <BoxView HeightRequest="20" />
            <Entry Text="{Binding Description, Mode=TwoWay}" />
        </StackLayout>
        <ListView ItemsSource="{Binding Subtasks}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Entry Text="{Binding Name, Mode=TwoWay}" 
                       FontSize="Small"                
                       TextColor="Red"               
                       HeightRequest="40" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <Button Text="Add subtask" Command="{Binding AddCommand}"></Button>
        <Button Text="Update" Command="{Binding UpdateCommand}"></Button>
        <Button Text="Delete" Command="{Binding DeleteCommand}" BackgroundColor="Red"></Button>
    </StackLayout>

</ContentPage>